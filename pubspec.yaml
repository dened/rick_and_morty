name: rick_and_morty
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: "^3.9.0"
  flutter: "^3.35.1"


workspace:
  - packages/ui

dependencies:
  flutter:
    sdk: flutter

  ui:
    path: packages/ui

  # Flutter dependenciew
  meta: any
  collection: any
  intl: any

  # Utility

  path_provider: ^2.1.5

  # Network
  http: ^1.5.0

  # Logger
  l: ^5.0.0

  # Database (SQLite)
  drift: ^2.28.1
  drift_flutter: ^0.2.5

  # Firebase
  firebase_auth: ^6.0.1
  firebase_core: ^4.0.0
  google_sign_in: ^7.1.1
  cloud_firestore: ^6.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting
  lints: ^6.0.0

  # Code generation
  build_runner: ^2.6.0
  drift_dev: ^2.28.1
  # json_serializable: ^6.9.5 
  # intl_translation: ^0.20.1

flutter:
  uses-material-design: true
